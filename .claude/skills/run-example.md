# Run goTS Example

Build and run the example program to test the full pipeline.

## Usage
Run this skill to verify the entire compilation and execution pipeline works.

## Command
```bash
cd /Users/yang/workspace/quickts/gots && go run ./cmd/gots run test/example.gts
```

## When to use
- After making changes to parser, compiler, or VM
- To verify language features work end-to-end
- To test new functionality
