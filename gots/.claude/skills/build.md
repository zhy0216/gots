# Build GoTS

Build the GoTS CLI executable.

## Usage
Run this skill to build the gots binary.

## Command
```bash
cd /Users/yang/workspace/quickts/gots && go build -o gots ./cmd/gots
```

## Output
Creates `gots` executable in the gots directory.
